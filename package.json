{
  "private": true,
  "name": "node-examination",
  "version": "0.0.1",
  "description": "Building a RESTful CRUD API with Node.js, Express/Koa and MongoDB.",
  "main": "src/server.js",
  "scripts": {
    "prepare": "husky install",
    "eslint": "eslint src/",
    "mocha": "cross-env NODE_ENV=test mocha src/test --parallel --recursive",
    "nyc": "cross-env NODE_ENV=test nyc npm run mocha"
  },
  "devDependencies": {
    "eslint": "^7.26.0",
    "husky": "^6.0.0",
    "lint-staged": "^11.0.0",
    "mocha": "^8.4.0",
    "nyc": "^15.1.0",
    "supertest": "^6.1.3"
  },
  "engines": {
    "node": ">=10.15.0"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
