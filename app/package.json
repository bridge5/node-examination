{
  "private": true,
  "name": "node-examination",
  "version": "0.0.1",
  "description": "Building a RESTful CRUD API with Node.js, Express/Koa and MongoDB.",
  "main": "server.js",
  "scripts": {
    "start": "NODE_ENV=development node server.js",
    "start:prod": "NODE_ENV=production node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "mocha-test": "mocha --no-timeouts --recursive ./test/player.test.js"
  },
  "dependencies": {
    "config": "^3.3.6",
    "eslint": "^7.21.0",
    "express": "^4.17.1",
    "mongodb": "^3.6.4",
    "mongoose": "^5.9.2",
    "request": "^2.88.2",
    "thenjs": "^2.1.0"
  },
  "devDependencies": {
    "chai": "^4.2.0"
  },
  "engines": {
    "node": ">=10.15.0"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
